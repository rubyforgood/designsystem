# Videos

- **`casa_old.mp4`** — a recording of [CASA](https://github.com/rubyforgood/casa) before its UI
  was migrated onto a design system, kept as before/after reference material alongside this
  system's docs. Re-encoded from the original ~112MB capture (H.264 CRF 18, faststart) to fit
  under GitHub's 100MB file-size limit; the `rubyforgood` org has Git LFS disabled, so this is a
  normal tracked file rather than an LFS pointer.
